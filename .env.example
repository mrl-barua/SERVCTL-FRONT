# SERVCTL Frontend environment variables
# Copy this file to .env and adjust as needed

# Backend API base URL used by Vite at build time
VITE_API_URL=http://localhost:3000

# Frontend mode flag
VITE_SERVCTL_MODE=local

# Host port used by docker-compose.yml
FRONTEND_PORT=80
